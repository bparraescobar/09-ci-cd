name: 01 - Hello World

on:
    push:
        branches: [main]
  
jobs:
    hello:
        runs-on: ubuntu-24.04
        steps:
            - name: saludar
              run: echo "Hello World!"

            - name: Ver fecha del runner
              run: date

            - name: Ver archivos disponibles
              run: ls -la